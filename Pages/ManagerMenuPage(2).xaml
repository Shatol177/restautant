<Page x:Class="Restaurant.Pages.ManagerMenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Restaurant.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Manager Menu">

    <Grid>
        <Button x:Name="BtnDishiStatistics" Style="{StaticResource MenuButton}" Content="Dish statistics" Click="BtnStatistics_Click" Margin="305,200,295,200"></Button>
        <Button x:Name="BtnEmployeeCards" Style="{StaticResource MenuButton}" Content="Employee cards" Click="BtnCardEmployee_Click" Margin="530,200,70,200"></Button>
        <Button x:Name="BtnClientsCards" Style="{StaticResource MenuButton}" Content="Clients cards" Click="BtnCardClients_Click" Margin="67,200,533,200"></Button>
    </Grid>
    
</Page>
